var React = require('react');
var ReactDOM = require('react-dom');


var RubricListContainer= require('./components/RubricListContainer');

ReactDOM.render(
  <RubricListContainer />,
  document.getElementById('main')
);
